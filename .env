# Auth Secret
BETTER_AUTH_SECRET=1521962fe0c48ef6fffd0068d491bdeb8d6a5be0fa43c86fb93ac4c371d21576

# Better Auth URL
BETTER_AUTH_URL=http://localhost:3000

# Database connection URL for PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres


# Session Configuration
COOKIE_DOMAIN=localhost


# SMTP Configuration
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USER=alex404hm@alexander-holm.com
SMTP_PASS=Cpr34aeb.,
SMTP_SECURE=true


# Login Security Thresholds (these match LOGIN_SECURITY_CONFIG defaults)
# Number of failed attempts before account lockout
LOGIN_LOCKOUT_THRESHOLD=5
# Account lockout duration in minutes
LOGIN_LOCKOUT_DURATION=30
# Base progressive delay in milliseconds (attempt 1=150ms ... attempt 6=900ms)
LOGIN_RATE_LIMIT_DELAY_MS=150

# Failed login tracking window (minutes)
LOGIN_ATTEMPT_WINDOW=30
# IP/device rate-limit window (minutes)
LOGIN_RATE_LIMIT_WINDOW_MINUTES=30
# Max failed attempts per IP during window before temporary 30-min block
LOGIN_IP_RATE_LIMIT_MAX_ATTEMPTS=20
# Max failed attempts per device fingerprint during window before temporary 30-min block
LOGIN_DEVICE_RATE_LIMIT_MAX_ATTEMPTS=15


# CORS & Trusted Origins
NEXT_PUBLIC_APP_URL=http://localhost:3000